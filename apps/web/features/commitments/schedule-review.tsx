"use client";

import { useMutation, useQueryClient } from "@tanstack/react-query";
import { CalendarCheck2, CheckCircle2, Clock3, XCircle } from "lucide-react";
import { useRef, useState } from "react";

import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Field, Input } from "@/components/ui/form-controls";
import { Panel } from "@/components/ui/panel";
import { applySchedule, previewCommitmentSchedule } from "@/lib/api/connected";
import { queryKeys } from "@/lib/api/query-keys";
import type { SchedulingPreview } from "@/lib/api/types";
import { formatDateTime, formatDuration, fromDateTimeLocal, toDateTimeLocal } from "@/lib/format";
import { useUiStore } from "@/stores/ui-store";

export function ScheduleReview({ commitmentId, timezone }: { commitmentId: string; timezone: string }) {
  const now = new Date();
  const [start, setStart] = useState(toDateTimeLocal(now.toISOString(), timezone));
  const [end, setEnd] = useState(toDateTimeLocal(new Date(now.getTime() + 14 * 86_400_000).toISOString(), timezone));
  const [preview, setPreview] = useState<SchedulingPreview | null>(null);
  const [reviewed, setReviewed] = useState(false);
  const pushToast = useUiStore((state) => state.pushToast);
  const queryClient = useQueryClient();
  const calculationController = useRef<AbortController | null>(null);
  const calculate = useMutation({ mutationFn: () => { calculationController.current?.abort(); const planningStart = fromDateTimeLocal(start, timezone); const planningEnd = fromDateTimeLocal(end, timezone); if (!planningStart || !planningEnd || new Date(planningEnd) <= new Date(planningStart)) throw new Error("The planning end must be after its start."); const controller = new AbortController(); calculationController.current = controller; return previewCommitmentSchedule(commitmentId, { planning_start_at: planningStart, planning_end_at: planningEnd, solver_time_limit_seconds: 2 }, controller.signal); }, onSuccess: (value) => { setPreview(value); setReviewed(false); }, onError: (error) => { if (!(error instanceof DOMException && error.name === "AbortError")) pushToast({ title: "Couldn't calculate a schedule", description: error instanceof Error ? error.message : "Try a shorter planning window.", tone: "error" }); }, onSettled: () => { calculationController.current = null; } });
  const apply = useMutation({ mutationFn: () => { if (!preview) throw new Error("Calculate a preview first."); return applySchedule({ preview_id: preview.preview_id }); }, onSuccess: async (result) => { pushToast({ title: `${result.placements.length} task placement${result.placements.length === 1 ? "" : "s"} applied`, tone: "success" }); await Promise.all([queryClient.invalidateQueries({ queryKey: queryKeys.tasks.all }), queryClient.invalidateQueries({ queryKey: queryKeys.calendar.all }), queryClient.invalidateQueries({ queryKey: queryKeys.scheduling.all }), queryClient.invalidateQueries({ queryKey: queryKeys.commitments.all }), queryClient.invalidateQueries({ queryKey: queryKeys.timeline.all })]); setPreview(null); setReviewed(false); }, onError: (error) => pushToast({ title: "Schedule wasn't applied", description: error instanceof Error ? error.message : "Source records may have changed. Calculate a fresh preview.", tone: "error" }) });
  return <Panel className="overflow-hidden"><div className="border-b border-border px-5 py-4"><h3 className="text-sm font-semibold">Scheduling preview</h3><p className="mt-1 text-xs leading-5 text-muted-foreground">Propose placements for linked tasks. Nothing changes until you review and apply.</p></div><div className="space-y-5 p-5"><div className="grid gap-4 sm:grid-cols-2"><Field id="schedule-start" label="Planning starts"><Input id="schedule-start" onChange={(event) => setStart(event.target.value)} type="datetime-local" value={start} /></Field><Field id="schedule-end" label="Planning ends"><Input id="schedule-end" onChange={(event) => setEnd(event.target.value)} type="datetime-local" value={end} /></Field></div><div className="flex flex-wrap gap-2"><Button loading={calculate.isPending} onClick={() => calculate.mutate()} type="button" variant="secondary"><CalendarCheck2 aria-hidden="true" className="h-4 w-4" />Calculate preview</Button>{calculate.isPending ? <Button onClick={() => calculationController.current?.abort()} type="button" variant="ghost"><XCircle aria-hidden="true" className="h-4 w-4" />Cancel calculation</Button> : null}</div>{preview ? <div className="space-y-5 border-t border-border pt-5"><div className="grid gap-3 sm:grid-cols-3"><div className="rounded-lg bg-muted p-4"><p className="text-xs text-muted-foreground">Solver</p><p className="mt-1 font-semibold">{preview.solver_status}</p></div><div className="rounded-lg bg-muted p-4"><p className="text-xs text-muted-foreground">Proposed</p><p className="mt-1 font-semibold tabular-nums">{preview.placements.length} tasks</p></div><div className="rounded-lg bg-muted p-4"><p className="text-xs text-muted-foreground">Still unscheduled</p><p className="mt-1 font-semibold tabular-nums">{preview.unscheduled_tasks.length}</p></div></div>{preview.placements.length ? <ol className="space-y-2">{preview.placements.map((placement) => <li className="grid gap-2 rounded-lg border border-border p-3 text-sm sm:grid-cols-[1fr_auto]" key={placement.task_id}><div><p className="font-medium">{placement.title}</p><p className="mt-1 text-xs text-muted-foreground"><Clock3 aria-hidden="true" className="mr-1 inline h-3 w-3" />{formatDateTime(placement.starts_at, timezone)} → {formatDateTime(placement.ends_at, timezone)}</p></div><Badge tone={placement.preference_satisfied ? "success" : "warning"}>{formatDuration(placement.duration_minutes)}</Badge></li>)}</ol> : <p className="rounded-lg bg-muted p-4 text-sm text-muted-foreground">No task placements were proposed. Review the unscheduled explanations below.</p>}{preview.unscheduled_tasks.length ? <div><h4 className="text-sm font-semibold">Why work remains unscheduled</h4><ul className="mt-2 space-y-2">{preview.unscheduled_tasks.map((task) => <li className="rounded-lg border border-warning/35 bg-warning/5 p-3 text-sm" key={task.task_id}><p className="font-medium">{task.title}</p>{task.reasons.map((reason) => <p className="mt-1 text-xs leading-5 text-muted-foreground" key={reason.code}>{reason.message}</p>)}</li>)}</ul></div> : null}<label className="flex cursor-pointer items-start gap-3 rounded-lg border border-border p-4 text-sm"><input checked={reviewed} className="mt-0.5 h-4 w-4 accent-foreground" onChange={(event) => setReviewed(event.target.checked)} type="checkbox" /><span><span className="font-medium">I reviewed every proposed placement</span><span className="mt-1 block text-xs leading-5 text-muted-foreground">Apply will update only the listed tasks and will stop if any source revision changed.</span></span></label><Button disabled={!reviewed || !preview.placements.length} loading={apply.isPending} onClick={() => apply.mutate()} type="button"><CheckCircle2 aria-hidden="true" className="h-4 w-4" />Apply reviewed schedule</Button></div> : null}</div></Panel>;
}
